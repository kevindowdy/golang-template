// Package tests holds integration-style tests that exercise the application
// as a whole. Unit tests for a package live alongside that package's source
// (e.g. src/internal/example/example_test.go), per Go convention.
package tests

import "testing"

// TODO: replace with a real integration test.
func TestPlaceholder(t *testing.T) {
	t.Skip("no integration tests yet")
}
