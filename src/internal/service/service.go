// Package service holds application/business logic. It orchestrates
// database and utilities to implement use cases, independent of how
// those use cases are invoked (HTTP handler, CLI command, etc.).
package service

// TODO: replace with real business logic.
