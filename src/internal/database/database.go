// Package database holds data access code: connection setup, queries, and
// repositories. Keep SQL/driver details here so callers in service depend
// on an interface, not a specific database.
package database

// TODO: replace with a real connection/repository type.
